# 관계형 테이블 : 관련있는 데이터들끼리 연관성을 묶어놓되, 선택적으로 이를 노출함으로써 여러 용도로 사용할 수 있게 한다.

# 회원가입 시 생성 시점에 해당 유저를 분리하는 ID 값을 발급하고 그에 따른 아이디값을 관계형 테이블 데이터에 ID값에 할당함으로써
이를 KEY값으로 여러 데이터를 불러올 수 있도록 한다.

# 로그인 성공 시 토큰이 발급된다. (입장권 같은 개념)
    경로 : 개발자 도구 > Application > Cookie or Local storage